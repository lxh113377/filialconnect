# Security Policy

FilialConnect 是纯静态站：没有账号系统、没有后端、没有表单接收端。页面只从本站自身取
资源——第三方库（i18next、workbox）随仓库入库并锁版本，不做任何远程拉取，也不加载网络字体。

## 本站收集什么

| 数据 | 存放位置 | 上传 |
|------|---------|------|
| 语言偏好、字号档位 | 浏览器 `localStorage` | 否 |
| 家人称呼 / 电话 / 邮箱 | 浏览器 `localStorage`（仅本机） | 否 |
| 求助表单内容 | 直接交给本机短信/邮件 App 生成草稿 | 否，需用户自己点发送 |
| 可疑链接自查输入 | 只在浏览器内存中比对离线名单 | 否 |

清空方式：删除站点数据（浏览器设置 → 存储），或在本机重新填写联系方式。

## 第三方数据

防骗域名名单来自 destroylist（MIT），随仓库打包为静态文件，运行时不再向第三方发起请求。
来源与许可见 [SOURCES.md](SOURCES.md)。

## 报告漏洞

本站不上传任何数据，因此风险面主要是**文案是否如实**（对老年用户最重要）
与**构建链供应链**。具体包括：

- 页面文案声称了代码没有实现的行为（这会让老年用户误判，属于最高优先级）；
- 任何把本机数据发往网络的改动；
- 依赖与 GitHub Action 的供应链问题。

请通过 [GitHub Security Advisory](https://github.com/lxh113377/filialconnect/security/advisories/new)
私下上报，不要开公开 issue。维护者会在 7 天内回复。
