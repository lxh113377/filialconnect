# OS
Thumbs.db
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Node (CI tooling only, not app deps)
node_modules/
.lighthouseci/
.lh-tmp/

# Archives
*.zip

# Python tooling
__pycache__/
*.pyc

# Internal working docs (project memory / audit reports) — kept out of the
# public repository; live one level up in 孝心联/_internal/
/_internal/

# workbox source maps (regenerate on demand)
*.map

# Search corpus + Pagefind index: build outputs (node tools/build-search.mjs index).
# Not shipped until assets/js/search.js consumes them, so a 1.3 MB index does not
# ride along in the Pages artifact or the competition ZIP.
_search/
pagefind/
