# 数据源方法论（SOURCES）

本项目的「防骗」内容由两部分组成，收录标准不同，可信度也不同。
读者（尤其老年用户与其子女）应能据此判断每条信息从何而来。

## 1. 中文骗局案例库（自采编，5 类）

- 位置：`content/fraud-cases.json` → 渲染进 `pages/fraud-database.html`
- 来源：中国公安与反诈宣传部门公开提示的常见话术类型（冒充公检法、冒充子女、
  中奖退款、养生保健品、刷单返利），逐条改写成老年可读的中文表述。
- 采编规则：
  1. 只收录**手法**，不收录具体号码/网址/APP 名（这些变化快，写进页面必然过期）；
  2. 每条必须给出「识别信号」与「马上做什么」两段，缺一不可；
  3. 不使用「所有」「一定」等全称承诺——`linkcheck.ok` 文案即为此而写。
- 更新方式：人工。修改 JSON 后执行 `python tools/build.py build`，
  CI 的 `build.py check` 会拒绝任何手工改产物造成的漂移。

## 2. 国际钓鱼域名名单（外部上游）

- 上游：`phishdestroy/destroylist`（MIT 协议，日更）的 rootlist
- 拉取：`python tools/fetch-fraud-feeds.py`（写 `assets/data/destroylist-domains.txt`
  与 `assets/data/fraud-feeds-meta.json`）
- 当前入库：83,097 条（其中 66,680 条是根域、16,417 条是上游按主机名收录的深层条目），
  `fraud-feeds-meta.json` 记录上游 commit、拉取时间，以及快照自身的 `snapshot_sha256`（防手工改动 / 截断）
- 匹配规则（`assets/js/main.js` 的 `listed()`）：把用户粘贴的 host 从完整主机名逐级上溯到
  根域，任一级命中即判危险。**只比"末两段"是不够的**——那样 19.8% 的深层条目只能靠逐字
  粘贴才可能命中。该规则由 `tools/test_build.py` 直接抽取浏览器实际加载的函数在 node 里
  跑真实名单验证（含反向对照：`zz.staging.store.lilybharat.com` 旧规则漏、新规则中）
- 离线审计：`python tools/fetch-fraud-feeds.py --check`（不联网）复核摘要、条数与
  新鲜度（>30 天判失败）；CI 每次构建都跑，因此定时任务停摆会直接变红而不是悄悄过期
- 档位说明：取上游 **online 根域档**（DNS 实测在线，`rootlist/online_root_domains.txt`）；上游另有含未审条目的 primary 全量档（21 万+），未采用——宁缺毋滥，避免未审条目造成误报
- **已知覆盖边界（必须如实告知用户，页面已写入 `linkcheck.scope`）**：
  - 名单以英文/国际钓鱼域为主，**中国大陆域名覆盖极薄**（`.cn` 653 条 / `.com.cn` 387
    条，合计 0.79%，2026-09-24 实测）；
  - 不覆盖电话诈骗、短信诱饵、微信/APP 内话术——这些正是中文场景的高发形态；
  - 因此「未命中」只表示「不在该英文名单里」，绝不等于安全。
- 为什么不用上游 API 实时查询：本站要求离线可用（竞赛交付 + 老年用户可能在
  弱网环境）。因此选择「随仓库打包静态快照」，代价是新鲜度靠定时任务维护。
- 误报处理：上游 destroylist 自身有申诉通道；本项目不自建收录，因此不存在
  本站层面的误报名单。若用户认为某条被误判，应向上游申诉，同时页面上的
  「未命中≠安全」文案已提供兜底解释。
- 许可合规：MIT，允许再分发，需保留版权声明。`fraud-feeds-meta.json` 保留来源署名；
  页面上 `linkcheck.note` 亦标注来源与协议。

## 3. 不在数据源范围内的内容

教程步骤里的第三方 App 名称（微信、12306、银行 App 等）为注册商标，仅作指认用途，
不意味着任何官方关联。界面与文案未使用任何第三方素材、图片或字体（全站内联 SVG +
系统字体栈）。
